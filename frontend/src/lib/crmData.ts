/**
 * crmData.ts — Batch 3 (stub / legacy cleared)
 *
 * All operational mock data has been removed.
 * Real data comes from the backend API via crmApi.ts.
 *
 * This file is kept as an empty stub so any legacy import that was
 * not yet cleaned up does not crash the build with a missing module
 * error. Nothing should import from here in production code.
 */

// No exports. File intentionally empty.
export {};
